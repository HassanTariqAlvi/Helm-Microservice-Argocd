apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: microservices-appset
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
<<<<<<< Updated upstream
    - list:
        elements:
          # MySQL Database
          - name: mysql
            path: mysql-helmchart
            namespace: default
            valuesFile: ""
            
          # API Service
          - name: api-service
            path: app-helmchart
            namespace: default
            valuesFile: values/api-service.yaml
            
          # Front Service
          - name: front-service
            path: app-helmchart
            namespace: default
            valuesFile: values/front-service.yaml
            
          # Option Service
          - name: option-service
            path: app-helmchart
            namespace: default
            valuesFile: values/option-service.yaml
            
          # Add more services here following the same pattern:
          # - name: your-service-name
          #   path: app-helmchart
          #   namespace: microservices
          #   valuesFile: values/your-service.yaml

=======
  - list:
      items:
      - name: api-service
        valuesFile: "values/api-service.yaml"  # ← INSIDE app-helmchart
      - name: front-service
        valuesFile: "values/front-service.yaml"  # ← INSIDE app-helmchart
      - name: option-service
        valuesFile: "values/option-service.yaml"  # ← INSIDE app-helmchart
  
>>>>>>> Stashed changes
  template:
    metadata:
      name: '{{.name}}'
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/HassanTariqAlvi/Helm-Microservice-Argocd.git
        targetRevision: main
<<<<<<< Updated upstream
        path: '{{.path}}'
=======
        path: app-helmchart
>>>>>>> Stashed changes
        helm:
          releaseName: '{{.name}}'
          valueFiles:
<<<<<<< Updated upstream
            - '{{if .valuesFile}}{{.valuesFile}}{{else}}values.yaml{{end}}'
=======
          - '{{valuesFile}}'  # This resolves to "values/api-service.yaml"
>>>>>>> Stashed changes
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{.namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
